package main

import (
	"salo"
	"gorilka"
        "balalaika"
)
func main() (w balaika) (
          if salo + gorilka = true (
                        return

          )

          slava ukraine
          ))
          
)                
